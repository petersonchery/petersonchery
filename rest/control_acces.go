package rest

/*
import (
    "yourapp/models"
    "yourapp/database"
)



func CanCreateBook(userID int) (bool, error) {
    user, err := database.GetUserByID(userID)
    if err != nil {
        return false, err
    }

    role, err := database.GetUserRoleByID(user.RoleID)
    if err != nil {
        return false, err
    }

    if role.RoleName == "Administrateur Locataire" {
        hasPermission, err := database.CheckUserPermission(userID, "creerLivre")
        if err != nil {
            return false, err
        }
        return hasPermission, nil
    }

    return false, nil
}
*/
